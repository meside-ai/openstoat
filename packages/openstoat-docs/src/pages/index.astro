---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const docs = await getCollection('docs');
const blogs = await getCollection('blogs');

const sidebarItems = [
  ...docs.map((d) => {
    const s = d.id.replace(/\.md$/, '');
    return { href: `/docs/${s}`, label: d.data.title ?? s, active: false };
  }),
  ...blogs.map((b) => {
    const s = b.id.replace(/\.md$/, '');
    return { href: `/blogs/${s}`, label: b.data.title ?? s, active: false };
  }),
];
---

<Layout title="OpenStoat" sidebarItems={sidebarItems} currentSection="home">
  <div class="prose dark:prose-invert max-w-none">
    <div class="mb-12">
      <h1 class="text-4xl font-bold tracking-tight">OpenStoat</h1>
      <p class="text-xl text-slate-600 dark:text-slate-400 mt-2">
        Agent-first AI and Human collaborative task orchestration system.
      </p>
      <p class="mt-4 text-slate-600 dark:text-slate-400">
        Local-first, CLI-first. No account or API key required. OpenStoat orchestrates tasks between AI agents and humans—agents execute by default, humans join when needed.
      </p>
      <div class="flex gap-4 mt-6 not-prose">
        <a href="/docs" class="inline-flex items-center px-4 py-2 rounded-lg bg-slate-800 dark:bg-slate-200 text-white dark:text-slate-900 font-medium hover:opacity-90 transition">
          Documentation
        </a>
        <a href="/blogs" class="inline-flex items-center px-4 py-2 rounded-lg border border-slate-300 dark:border-slate-600 text-slate-700 dark:text-slate-300 font-medium hover:bg-slate-100 dark:hover:bg-slate-800 transition">
          Blog
        </a>
      </div>
    </div>
    <section>
      <h2>Quick Links</h2>
      <div class="grid gap-6 sm:grid-cols-2 not-prose mt-4">
        <a href="/docs" class="block p-4 rounded-lg border border-slate-200 dark:border-slate-700 hover:border-slate-400 dark:hover:border-slate-500 transition">
          <h3 class="font-semibold text-slate-900 dark:text-slate-100">Documentation</h3>
          <p class="text-sm text-slate-600 dark:text-slate-400 mt-1">Product spec, architecture, use cases, and development guides.</p>
        </a>
        <a href="/blogs" class="block p-4 rounded-lg border border-slate-200 dark:border-slate-700 hover:border-slate-400 dark:hover:border-slate-500 transition">
          <h3 class="font-semibold text-slate-900 dark:text-slate-100">Blog</h3>
          <p class="text-sm text-slate-600 dark:text-slate-400 mt-1">Stories, updates, and insights about OpenStoat.</p>
        </a>
      </div>
    </section>
  </div>
</Layout>
