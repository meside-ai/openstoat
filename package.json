{
  "name": "palmlist",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/palmlist-types && bun run build && cd ../palmlist-core && bun run build && cd ../palmlist-daemon && bun run build && cd ../palmlist-web && bun run build && cd ../palmlist-cli && bun run build",
    "test": "bun test",
    "test:integration": "bun test tests/integration",
    "palmlist": "bun run packages/palmlist-cli/dist/index.js",
    "web": "bun run packages/palmlist-web/src/index.ts",
    "web:dev": "bun run --watch packages/palmlist-web/src/index.ts",
    "docs": "cd packages/palmlist-docs && bun run dev",
    "link:global": "bun run build && cd packages/palmlist-cli && npm link --force",
    "docs:build": "cd packages/palmlist-docs && bun run build",
    "docs:preview": "cd packages/palmlist-docs && bun run preview"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}